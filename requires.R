#install.packages("Amelia")
# This package is for the missmap plot to visualize overall dataset
require(Amelia)
#This package required for doing a Corrgram
require(corrgram)
#This package required for
require(Hmisc)
#This package required for
require(plyr)     # for the revalue function
#This package required for
require(stringr)  # for the str_sub function
#

require (pROC)
require(caret)
